/**
 * ⚠️ 这是结构参考，不是死板模板！
 *
 * 实际测试根据练习内容动态生成：
 * - 测试用例覆盖练习的关键点
 * - 边界情况根据知识点特性设计
 * - 错误提示帮助学生理解问题
 */

import { describe, it, expect } from 'vitest';
// 导入学生需要实现的函数/组件

describe('{主题} 练习', () => {
  describe('练习 1：{标题}', () => {
    it('{正常情况测试描述}', () => {
      // 测试代码
      // expect(studentAnswer).toBe(expectedValue);
    });

    it('{边界情况测试描述}', () => {
      // 边界情况测试
    });

    // 如果学生在这个概念上有困惑，增加更多测试用例帮助理解
  });

  describe('练习 2：{标题}', () => {
    it('{测试描述}', () => {
      // 测试代码
    });
  });

  // 根据练习数量动态添加更多 describe 块
});

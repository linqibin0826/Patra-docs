---
title: 条件渲染
chapter: 02
section: 07
status: 已完成
progress:
  started_at: 2025-12-06
  completed_at: 2025-12-06
  time_spent: "1h"
  mastery: 熟悉
tags:
  - learning/react/ch02
  - tech/react
  - tech/jsx
---

# 条件渲染

> 根据状态的不同，渲染不同的 UI —— 这是让组件「智能」起来的关键。

---

## 📋 学习目标

- [x] 理解 JSX 中只能使用表达式，不能使用语句
- [x] 掌握三元表达式实现二选一渲染
- [x] 掌握逻辑与 `&&` 实现显示/隐藏
- [x] 掌握提前 return 处理多分支场景
- [x] 避开 `&&` 的 0 陷阱

---

## 📖 知识点

### 1. 为什么不能用 if 语句？ ⭐

JSX 的 `{}` 里只能放**表达式**，不能放**语句**：

```
┌─────────────────────────────────────────────────┐
│  表达式 (Expression)    →  有返回值，可以放     │
│  • 1 + 1                                        │
│  • isLoggedIn ? 'Hi' : 'Login'                  │
│  • count > 0 && <Badge />                       │
├─────────────────────────────────────────────────┤
│  语句 (Statement)       →  没有返回值，不能放   │
│  • if (...) { }                                 │
│  • for (...) { }                                │
│  • switch (...) { }                             │
└─────────────────────────────────────────────────┘
```

**类比 Java**：就像注解参数里不能写 if 语句一样 —— 只能是编译期常量表达式。

---

### 2. 三元表达式 `? :` ⭐

**适用场景**：二选一，显示 A 或显示 B

```tsx
// 切换文字
<button>{isLoggedIn ? '退出' : '请登录'}</button>

// 切换整个元素
{isLoggedIn ? (
  <>
    <span>欢迎回来！</span>
    <button>退出</button>
  </>
) : (
  <button>请登录</button>
)}
```

> 💡 `<>...</>` 是 **Fragment**，用来包裹多个元素但不产生额外的 DOM 节点。

---

### 3. 逻辑与 `&&` ⭐

**适用场景**：显示或不显示（不是二选一）

```tsx
// 有消息时才显示徽章
{count > 0 && <span className="badge">{count}</span>}

// 有错误时才显示提示
{error && <div className="error">{error.message}</div>}
```

**原理**：JavaScript `&&` 的短路求值

```
┌─────────────────────────────────────────────────┐
│  左边 truthy  →  返回右边的值                    │
│  左边 falsy   →  返回左边的值（短路）            │
└─────────────────────────────────────────────────┘
```

---

### 4. `&&` 的 0 陷阱 ⚠️

这是条件渲染中**最常见的坑**：

```tsx
// ❌ 错误！count 为 0 时页面会显示 0
{count && <Badge count={count} />}

// ✅ 正确：用比较表达式
{count > 0 && <Badge count={count} />}
```

**为什么？**

```
count = 0（falsy）
    ↓
0 && <Badge />
    ↓
返回 0（不是 false！）
    ↓
React 把 0 渲染到页面上
```

**关键理解**：
- `0` 和 `""` 是 falsy，但 React 会把它们**渲染出来**
- `false`、`null`、`undefined` 是 falsy，React **不会渲染**

---

### 5. 提前 return ⭐

**适用场景**：多个分支、复杂条件逻辑

```tsx
function UserCard({ isLoading, user }) {
  // 先处理异常情况
  if (isLoading) {
    return <div>加载中...</div>;
  }

  if (!user) {
    return <div>用户不存在</div>;
  }

  // 最后处理正常情况
  return (
    <div>
      <h1>{user.name}</h1>
      <p>{user.email}</p>
    </div>
  );
}
```

**优点**：
- 代码扁平化，避免 if-else 嵌套地狱
- 异常情况先处理，正常情况最后写
- 可读性好，逻辑清晰

---

## 🧠 三种方式对比

| 方式 | 语法 | 适用场景 | 示例 |
|------|------|----------|------|
| **三元** | `a ? b : c` | 二选一 | 登录/未登录 UI |
| **逻辑与** | `a && b` | 显示/隐藏 | 消息徽章、错误提示 |
| **提前 return** | `if () return` | 多分支 | 加载中/错误/空/正常 |

```
选择决策树：

需要条件渲染？
    │
    ├─ 二选一？ ──────→ 三元表达式
    │
    ├─ 显示或隐藏？ ──→ && （注意 0 陷阱）
    │
    └─ 多个分支？ ────→ 提前 return
```

---

## ✏️ 练习

完成以下练习：

| 练习 | 文件 | 知识点 |
|------|------|--------|
| 登录状态切换 | [07-conditional-rendering.tsx](idea://open?file=/Users/linqibin/Desktop/Patra/patra-react-playground/src/exercises/ch02/07-conditional-rendering.tsx&line=11) | 三元表达式 |
| 消息红点徽章 | [07-conditional-rendering.tsx](idea://open?file=/Users/linqibin/Desktop/Patra/patra-react-playground/src/exercises/ch02/07-conditional-rendering.tsx&line=38) | && + 避开 0 陷阱 |
| 用户资料卡片 | [07-conditional-rendering.tsx](idea://open?file=/Users/linqibin/Desktop/Patra/patra-react-playground/src/exercises/ch02/07-conditional-rendering.tsx&line=70) | 提前 return |

---

## 💡 学习反思

### 掌握较好的部分
- `&&` 的 0 陷阱理解透彻（通过预测练习发现）
- 提前 return 模式与 Java 的 Guard Clause 类似，容易上手
- 三种方式的适用场景区分清晰

### 需要注意的部分
- 三元表达式可以返回整个 JSX 元素，不只是文字
- Fragment `<>...</>` 用于包裹多个元素

### 核心心法
```
条件渲染 = JavaScript 条件表达式 + JSX
React 不发明新语法，拥抱 JavaScript 本身
```

---

## 🔗 导航

- 上一节：[[06-event-handling|事件处理]]
- 下一节：[[08-list-rendering|列表渲染与 key]]

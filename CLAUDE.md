# CLAUDE.md

Patra 项目文档仓库（Obsidian + Quartz）。

## 工作范围

**只操作 `content/` 目录**。查看 `content/CLAUDE.md` 了解文档编写规范。

## 禁止操作

不要查看或修改以下内容：

- `quartz/` — 框架源码
- `quartz.config.ts`、`quartz.layout.ts` — 站点配置
- `docs/` — Quartz 文档
- `package.json`、`*.d.ts`、`tsconfig.json` — 构建配置
- `.github/` — CI/CD 配置

## 语言规则

- 文档内容：中文
- 文件名：kebab-case 英文
